//Write a program that creates a binary file "numbers.bin" 
//and writes the numbers 1 to 5 into the file using fwrite(). 
//Then, reopen the file and read the data back into an array, 
//displaying each number on the console.

